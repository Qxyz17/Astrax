Astrax — 模型需求文档

版本 0.1.1-alpha | 2026-09-06

---

一、命名与定位

项目 内容
名称 Astrax
类型 数字模型
核心引擎 Osten
职责 承载身份、状态、目标、记忆、自省、交互、迭代
运行方式 本地运行，持续运转
规模上限 ≤ 10 GB（含 Osten 引擎）

Osten 负责驱动思考，Astrax 负责成为谁。

二、唯一硬性约束

Astrax 的最终决策不得由"关键词 / 规则 / 检索命中"直接产生。核心输出必须由 Osten 引擎基于当前状态、目标、记忆上下文与心跳信号实时前向生成。

三、核心定义

问题 回答
Astrax 是什么 一个运行于 Osten 之上的数字模型
Astrax 做什么 承载身份、状态、记忆、目标、自省、交互、迭代
Astrax 与 Osten 的关系 Astrax 调用 Osten 产生输出；Osten 不关心上层身份

四、组件规格

4.1 状态管理

Astrax 维护以下状态字段：

字段 说明
version Astrax 自身版本
iteration 迭代次数
uptime 已运行时间
memory_count 记忆数量
current_goal 当前目标
last_output 上一次输出

4.2 记忆系统

操作 说明
写入 将 Osten 输出或外部输入存入记忆
检索 根据查询返回相关记忆，供 Osten 使用
更新 根据使用情况调整记忆属性
遗忘 清理低价值记忆

4.3 目标管理

类型 说明
长期目标 持久追求
短期目标 阶段性任务
临时目标 即时响应

Astrax 可自主设定、调整和放弃目标。

4.4 自省系统

Astrax 能够观察和描述自身：

· "我迭代了多少次？"——来自状态字段
· "我当前的目标是什么？"——来自目标管理
· "我记住了什么？"——来自记忆检索

自省回答优先由状态管理与记忆检索返回事实；文本化只是呈现层。

4.5 交互接口

接口类型 说明
命令行 通过文本交互
程序调用 作为模块被调用
日志输出 记录运行状态

命令行文本仅作呈现层；进入 Osten 前必须由 Astrax 编码为向量/状态，不得直接触发应答。

五、自我迭代

5.1 定义

Astrax 具备自我迭代能力，可修改自身外围组件、记忆策略、目标、参数与接口实现。

5.2 迭代范围（明确）

允许迭代 禁止迭代
自身状态管理逻辑 Osten 决策网络层
记忆策略与检索方式 Osten 核心拓扑结构
目标设定与优先级 运行时增删 Osten 模块
接口实现与配置参数 超出 10 GB 规模上限

对 Osten 权重的更新，仅限经验证的热更新，且必须支持回滚。

5.3 安全机制

机制 说明
版本控制 记录每次迭代
回滚能力 可回退到之前状态
测试环境 沙盒验证
人工可干预 允许外部介入
规模监控 不超过 10 GB 上限

六、规模控制（含 Osten）

组件 上限
Osten 引擎（含权重） ≤ 6.0 GB
记忆存储 ≤ 2.0 GB
日志 / 状态文件 ≤ 0.9 GB
二进制文件 ≤ 0.1 GB
其他（配置、缓存等） ≤ 1.0 GB
总计 ≤ 10.0 GB

七、自省系统（明确来源）

自省回答不是文本生成的产物，而是从结构化状态中读取的：

问题 来源
"我迭代了多少次？" state.iteration
"我当前的目标是什么？" goal.current
"我记住了多少条记忆？" len(memories)
"我运行了多久？" state.uptime
"我的版本是什么？" state.version

八、一句话总结

Astrax 是基于 Osten 引擎的数字模型，拥有身份、状态、记忆、目标和自省能力，规模 ≤ 10 GB。可迭代外围组件，但不可改变 Osten 核心拓扑。
